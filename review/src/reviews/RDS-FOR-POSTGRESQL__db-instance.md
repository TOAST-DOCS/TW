---
service: RDS-FOR-POSTGRESQL
path: ko/db-instance.md
status: M
grade_change: O
grade_full: O
review_state: auto
---

# RDS-FOR-POSTGRESQL / ko/db-instance.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [변경분] 921행 새 절 제목 `### 데이터베이스 사용자 제어` → 수정 표(912행) `데이터베이스 & 사용자 제어`와 통일해 `### 데이터베이스 & 사용자 제어`로
- [ ] [기존] 617행 상태 표 `SHUTDOWN | 중지됨` → `정지됨`(stop=정지, PostgreSQL은 MySQL '중지' 예외 아님)
- [ ] [기존] 노트 박스(`> [주의]`/`> [참고]` 블록 인용, 519·522·659·925행 등 문서 전반) → MkDocs admonition(`!!! danger`/`!!! tip`)로 통일, 본문 4칸 들여쓰기
- [ ] [기존] 점 목록 불릿 `*`(527~533·680~681행 등 문서 전반) → `-`로 통일

## 참고
- 등급: ⓐ 변경분 = 수정 필요(경미) / ⓑ 문서 전체 = 수정 필요(경미)
- ⓐ 6월 diff는 대체로 양호: `가상 장비→가상 머신`, `1~100→1~100자`, `c2` 설명 정리, `고객 센터→고객지원`(523·337·442·1107·1308행), `로테이트 됩니다→로테이트됩니다`, `변경이 가능→변경할 수 있습니다`, `최대값→최댓값`·`한번→한 번`, `직전 한시간→직전 1시간`, `Floating IP→플로팅 IP`, `pg_dump  →pg_dump `, `'새로 고침'하세요→**새로 고침**을 클릭하세요` 모두 적절. (행번호는 CONTENT 기준)
- [참고] 신규 절(921~926행)의 `> [주의]` 노트는 잘못된 admonition 형식이나, 문서 전반이 동일한 `>` 블록 인용 스타일이라 별도 변경분 지적 대신 위 노트박스 통일 항목에 포함.
- [참고] `[...](...) 항목을 참고합니다`(560행 등) 다수는 링크 마무리 표현이라 `참고하세요` 강제 통일 대상에서 제외(원문 유지 룰).
- [참고] `복제 중단`(616·1084·1086행)의 '중단'은 '중지'(stop)와 다른 어휘(interruption)이므로 상태 용어 룰 미적용.
- [참고] 신규 절 직접 제어 권한(`CREATEDB`/`CREATEROLE` 부여, `CUSTOM` 권한 표현)은 기능 동작 의미라 문구 수정보다 작성자 확인 권장.
