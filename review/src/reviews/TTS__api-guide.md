---
service: TTS
path: ko/api-guide.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# TTS / ko/api-guide.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 77-79행 성공 응답 불릿 `*` → `-`
- [ ] [기존] 82행 실패 응답 단일 항목 `*` → `-`(단일 문장이므로 평문 검토)
- [x] [변경분] 35행 `* TTS API를` → `Text to Speech(TTS) API를`(불릿 제거·정식 명칭 보강), 적절
- [x] [변경분] 72행 `샘플링레이트`→`샘플링 레이트`, `경우엔`→`경우에는`, `고정되어야합니다`→`고정되어야 합니다.`, 적절

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 수정 필요(서식)
- 2차 검증: diff 2건은 모두 띄어쓰기·표기 개선으로 적절(행번호 확인). 기존 이슈는 응답 절 `*` 불릿뿐이며 용어/조사/링크/표·코드블록 이상 없음. 내부 상대 링크(`/nhncloud/...`)이고 통신사 고객센터·정착 영문 서비스명 등 오탐 가드 해당 없음.
