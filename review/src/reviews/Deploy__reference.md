---
service: Deploy
path: ko/reference.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# Deploy / ko/reference.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.
> 다음 빌드 때 대시보드 진행 상태에 반영됩니다.

## 변경해야 할 항목
- [ ] [기존] 309행 `참고하시기 바랍니다` → `참고하세요`
- [ ] [기존] 318행 `참고하시기 바랍니다` → `참고하세요`
- [ ] [기존] 429·430행 `타겟 디렉토리` → `타겟 디렉터리`(409·410행과 통일, 외래어 표기 `디렉터리`)
- [ ] [기존] 285행 `시나리오 구성 요소 입니다` → `시나리오 구성 요소입니다`
- [ ] [기존] 문서 전반 불릿 기호 `*` → `-`로 통일
- [x] [변경분] 221행 `Binary Upload API` 링크 URL을 `api-guide-v2.1/#_4`로 교체 — v2.1 가이드의 Binary Upload API 섹션 앵커가 `#_4`로 실제 도달 확인(WebFetch), 조치 불필요
- [ ] [참고] `Run As`/`RunAs`/`Run as`, `Jenkins Server`, `Target Server`, `Variable As`/`Variables As` 등은 콘솔 입력 폼 정착 라벨이라 통일 강제 안 함(라벨 일치만 점검)
- [ ] [참고] `배포/종료/재시작`, `실행 중단`/`무중단` 등 동작 의미 용어는 기술적 사실이라 임의 변경 금지
- [ ] [참고] 221행 `확인하실 수 있습니다`는 링크 마무리 표현이라 원문 유지(통일 강제 금지)

## 참고
- 등급: ⓐ 변경분 = 통과(링크 앵커 `#_4` 도달 확인) / ⓑ 문서 전체 = 수정 필요(어미·표기 통일 다수)
- 6월 변경은 링크 URL 1건뿐이며 앵커 도달이 확인되어 깨끗함.
- 기존 이슈: `참고하시기 바랍니다` 2건, `디렉토리`/`디렉터리` 혼용, 띄어쓰기 1건, 불릿 기호 `*`.
