---
service: notification-hub
path: ko/console-guide/address-book.md
status: M
grade_change: G
grade_full: R
review_state: auto
---

# notification-hub / ko/console-guide/address-book.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 168행 토큰 섹션인데 `이메일 도메인을 선택`·`수신 거부된 이메일 주소를 조회`로 서술(이메일 섹션 복붙) → 토큰 기준으로 정정
- [ ] [기존] 129행 `**+그룹 추가**` `+`와 글자 사이 공백 누락 → `**+ 그룹 추가**`
- [ ] [기존] 159행 `**+ 수신 거부 이메일 추가** 버튼 드롭다운 목록` `아래` 누락 → `버튼 아래 드롭다운 목록`
- [ ] [기존] 전체 불릿 기호 `*` 사용 → 룰셋 서식 기준 `-`로 통일
- [x] [변경분] 6월 diff는 `<a id="...">` 앵커·`pre-align` 주석 추가뿐, 본문 텍스트 변경 없음 — 조치 불필요
- [ ] [참고] 109행 `수신자(회원)`는 고객 회원 시스템 식별자 맥락 → `계정` 변환 금지(오탐 가드)
- [ ] [참고] 168행 정정 시 토큰의 실제 조회 단위(도메인/토큰)는 작성자·콘솔 확인 권장

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 시급(토큰 섹션 내용 오류)
- 6월 diff는 섹션별 HTML 앵커(`<a id>`)와 정렬 주석만 추가 → ⓐ=G. 전체 기준은 168행 토큰 섹션이 이메일 섹션을 복붙한 내부 모순(R)이 남아 ⓑ=R 유지.
- 부수 이슈: 버튼명 공백 불일치(129·159행), 불릿 `*`→`-` 통일.
- 168행은 텍스트 내부 모순이라 오류 자체는 실측 없이 명백하나, 정정 문구의 정확한 단위는 확인 권장.
