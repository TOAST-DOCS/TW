---
service: Storage-Block-Storage
path: ko/public-api.md
status: M
grade_change: O
grade_full: R
review_state: auto
---

# Storage-Block-Storage / ko/public-api.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터(TW)가 각 항목의 타당성을 검토합니다.
> 실제 원본 문서 수정은 이후 개발자가 수행합니다.

## 변경해야 할 항목
- [변경분] | 163, 223행 | `기본값은 1000으로 설정` | `1,000`(천 단위 콤마)
- [기존] | 474행 | `volumes.id` (생성 응답 표는 단수 `volume.*` 객체, 필드명 오류) | `volume.id`
- [기존] | 253행 | `블록 스토리지  그룹 ID`(공백 2칸·설명 누락) | 356행처럼 `블록 스토리지 컨시스턴시(일관성) 그룹 ID`
- [기존] | 582행 | `</br>` | `<br>`
- [기존] | 561행 | `> [주의]` 인용 | MkDocs `!!! danger`(본문 4칸 들여쓰기)
- [기존] | 44행 | `Block Storage은(는)` 자동치환 잔여 | `Block Storage는`
- [기존] | 123, 672행 | `상태 명` | `상태명` (`-명` 붙여 쓰기)
- [참고] | 163, 223행 | `limit` 기본값 `100, 최대 1000` → `1000으로 설정`으로 변경되며 '최대' 상한 표기가 사라짐 | 동작 의미 변화, 작성자 확인 필요
- [참고] | 657, 714행(diff) | 스냅숏 목록/상세 API에서 `limit` 행 삭제 | 파라미터 미지원 여부 작성자 확인 필요

## 참고
- 등급: ⓐ 변경분 = 수정 필요 / ⓑ 문서 전체 = 시급
- ⓑ를 R로 둔 근거: 474행 `volumes.id` 응답 필드명 오류(개발자가 코딩하는 스키마 오기재).
- diff 핵심은 `limit` 기본값 100→1000 변경과 스냅숏 API `limit` 행 삭제 2건. 둘 다 동작 의미가 달라지는 기술적 사실 변경이라 수정 대신 작성자 확인으로 분리. 숫자 콤마만 즉시 수정.
- 콘솔 메뉴/경로 실측 필요 지적 없음(순수 IaaS API 레퍼런스). 외부 깨진 http 링크 없음(엔드포인트 예시 URL·내부 상대링크만 존재).
