---
service: Compute-Instance
path: ko/public-api.md
status: M
grade_change: O
grade_full: R
review_state: auto
---

# Compute-Instance / ko/public-api.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [변경분] 499행 `지정된 개수 만큼` → `지정된 개수만큼` (`만큼` 붙여 쓰기)
- [ ] [기존] 500행 `첫번째` → `첫 번째` (띄어 쓰기)
- [ ] [기존] 500행 `개수 만큼` → `개수만큼` (`만큼` 붙여 쓰기)
- [ ] [기존] 475행 `UNKNOWN` 둘째 문장 `` `...관리자에게 문의합니다.` `` 가 백틱 코드로 감싸짐 → 평문으로
- [ ] [기존] 1474행 링크 경로에 공백 `/Storage/Block Storage/ko/public-api/...` → 공백 제거(링크 깨짐)
- [ ] [기존] 1601·1663·1701·1758·1784·1853행 `<a id>` 앵커가 해당 섹션(메타데이터·배치 정책)과 불일치 → 섹션에 맞는 앵커로 정정

## 참고
- 등급: ⓐ 변경분 = 수정 필요(O, 499행 띄어쓰기) / ⓑ 문서 전체 = 시급(R, 1474행 링크 깨짐 + 앵커 불일치)
- [참고] 499행 변경으로 `limit`의 기본값·최댓값(100/1000) 설명이 삭제됨 — API 동작 정보 손실, 작성자 확인 필요(복원 시 `최대 1,000`).
- [참고] 상태표 `SHUTOFF`=`중지`(473행)·`SHELVED_OFFLOADED`=`종료`(472행) 등은 OpenStack API 상태 의미와 결합된 표현 — stop=정지·중지 미사용 룰 일괄 적용 금지(작성자 확인).
- [참고] 857~858행 본문 `BUILDING`이 상태표(459행) `BUILD`와 표기 불일치 — 기술 표기, 작성자 확인.
- 1986행 API 레퍼런스라 ⓑ는 용어/조사/띄어쓰기/링크/앵커/표·코드블록 구조를 스캔. 표 구조·코드 예시는 양호.
