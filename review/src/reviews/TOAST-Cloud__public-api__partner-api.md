---
service: TOAST-Cloud
path: ko/public-api/partner-api.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# TOAST-Cloud / ko/public-api/partner-api.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 1852행 오류 코드 표 `이용 중인서비스`(2곳) → `이용 중인 서비스`
- [ ] [기존] 954행 `최종금액` → `최종 금액`(타 행은 `최종 금액`)
- [ ] [기존] 956행 `할인금액` → `할인 금액`(타 행은 `할인 금액`)
- [ ] [기존] 179행 `표시순서` → `표시 순서`(843행 표기와 통일)
- [x] [변경분] OCP → `Optimized Cost Plans(OCPs)` 약어 풀어쓰기(여러 표) — 적절
- [x] [변경분] `포함여부`→`포함 여부`, `조회방식`→`조회 방식`, `리소스 별로`→`리소스별로`, `몇개`→`몇 개` 띄어쓰기 정정 — 적절
- [x] [변경분] `구분 컬럼`→`구분 칼럼`(2곳) 외래어 표기 정정 — 적절
- [ ] [참고] 939·1504행 `회원 UUID`: 빌링 주체 식별자 필드, `계정 UUID` 표기 여부 작성자 확인
- [ ] [참고] 395·693·1002행 `돈통 유형` 내부 용어가 파트너 공개 문서에 노출 → 표준 용어 여부 작성자 확인

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 수정 필요
- 6월 diff는 OCP 약어 풀어쓰기·띄어쓰기·외래어 표기 정정뿐으로 모두 개선이며 기술적 사실 변경 없음 → ⓐ=G.
- ⓑ 지적은 모두 기존 띄어쓰기 일관성(최종금액/할인금액/표시순서/중인서비스) 🟠 수준. 표·코드블록·admonition 서식 일관, 링크 정상(엔드포인트 https), 필수 섹션 누락 없음 → R 근거 없음.
