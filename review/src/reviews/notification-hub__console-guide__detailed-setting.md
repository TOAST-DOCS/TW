---
service: notification-hub
path: ko/console-guide/detailed-setting.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# notification-hub / ko/console-guide/detailed-setting.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 303행 `**버킷이름**` → `**버킷 이름**`(띄어쓰기, 콘솔 UI 표기 기준)
- [ ] [기존] 179행 `시간동안` → `시간 동안`(띄어쓰기)
- [ ] [기존] 300행 `사용안함` → `사용 안 함`(157행 표기와 통일)
- [ ] [기존] 158행 링크 텍스트 `[[고객 센터]]` → `[[고객지원]]`(링크 대상이 NHN Cloud 지원 페이지, 실제 페이지 제목 '문의하기' 확인)
- [ ] [기존] 문서 전체 불릿 기호 `*` → `-`로 통일(룰셋 서식 규칙)
- [ ] [기존] 268~274행 한 목록 내 `*`/`-` 불릿 혼용 → `-`로 통일
- [x] [변경분] 226~242행 신규 `## 브랜드 메시지` 섹션 — 용어·조사·어미 이상 없음(기존 `*` 불릿 패턴 준수)
- [x] [변경분] 다수 `<a id="...">` 앵커 및 `pre-align` 주석 추가 — 구조적 변경, 본문 영향 없음

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 수정 필요
- 6월 diff는 `<a id>` 앵커 삽입·`pre-align` 주석·신규 `브랜드 메시지` 섹션 추가로 구성되며 변경분 자체에는 결함 없음(ⓐ=G).
- 158행 링크는 외부 https 도달 확인됨(깨진 링크 아님). 기존 본문 띄어쓰기·`*` 불릿·`고객 센터` 명칭 등 🟠만 남아 ⓑ=O(🔴 없음).
- 초안의 `사용함`→`사용` 지적은 과적용(문맥상 '사용함으로 설정 시' 서술어)이라 제외. `시간동안` 36행은 diff 컨텍스트 에코(=179행)라 1건으로 통합.
