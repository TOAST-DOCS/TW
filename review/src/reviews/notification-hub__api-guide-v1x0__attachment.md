---
service: notification-hub
path: ko/api-guide-v1x0/attachment.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# notification-hub / ko/api-guide-v1x0/attachment.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 339행 `업로드 할` → `업로드할` (붙여 쓰기, 동일 문서 114·433행은 `업로드할`)
- [x] [변경분] 1행 `<!-- pre-align:aligned sig=... -->` 빌드 메타 주석 추가 — 본문 영향 없음, 조치 불필요
- [x] [변경분] 122·230·335·439·551·663행 `<a id="...">` 앵커 추가 — 사용자 노출 텍스트 없음, 적절
- [ ] [참고] 193·297행 `파일명`과 144행 `파일 이름` 혼용 — `-명`↔`이름` 일괄 변환 금지(건별 판단), 작성자 확인
- [ ] [참고] 195·300행 `첨부 파일의 사이즈 단위는 byte`의 `사이즈` — 필드 설명 정착 표현, 작성자 확인

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 수정 필요(경미)
- 변경분은 빌드용 정렬 주석 1건과 섹션 앵커(`<a id>`) 6건 추가뿐으로 한글 본문·용어·기술 내용 변경이 없어 깨끗함(G).
- 전체(ⓑ)는 띄어쓰기 1건(`업로드할`)만 실질 🟠. 외부 깨진 링크·콘솔 경로·고객센터 표현 없음(이 문서는 API 레퍼런스). `미리보기`(299행) 붙여쓰기 정상. 예시 JSON의 `www.example.com` URL은 플레이스홀더로 검증 대상 아님.
