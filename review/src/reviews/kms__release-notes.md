---
service: kms
path: ko/release-notes.md
status: M
grade_change: O
grade_full: O
review_state: auto
---

# kms / ko/release-notes.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [변경분] 31-32행 신규 추가 불릿이 `*` 기호 → 룰셋 `*`/`+`→`-` 위반, `-`로 변경(문서 전반 동일)
- [x] [변경분] 53행 `... 추가 (v1.2, v1.3)` → `... 추가(v1.2, v1.3)` 괄호 앞 공백 제거, 적절
- [ ] [참고] 32행 등 링크 마무리 `를 참고.`는 문서 전반 정착 패턴 → 링크 마무리 원문 유지 룰에 따라 통일 강제 금지

## 참고
- 등급: ⓐ 변경분 = 수정 필요(경미) / ⓑ 문서 전체 = 수정 필요(경미)
- 6월 diff(2026.06.09 신규 섹션 추가, 53행 괄호 앞 공백 정리)는 H1/H2/H3 구조·부모 명사형은 준수하나, 추가된 불릿이 `*` 기호라 서식 룰 위반에 해당(문서 전체가 `*` 사용이므로 [기존]이자 [변경분]).
- 링크는 내부 절대경로 다수와 `https://` 외부 1건(229행 부근 공지)으로 도달 확인됨, 깨진 링크 없음. R 근거(기술 오류·링크 깨짐·필수 섹션 누락·콘솔 불일치) 없어 R 미부여. 글로서리·조사·콘솔 UI 불일치 없음.
