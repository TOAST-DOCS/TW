---
service: Vaccine
path: ko/console-guide-ahnlabcpp.md
status: M
grade_change: G
grade_full: O
review_state: auto
---

# Vaccine / ko/console-guide-ahnlabcpp.md — 자동 검수 결과

> Claude 자동 검수 초안입니다. 담당 라이터가 항목을 검토·반영(`[x]` 체크)하고,
> 완료되면 상단 `review_state`를 `done`(검토 중이면 `reviewing`)으로 바꿔 주세요.

## 변경해야 할 항목
- [ ] [기존] 137행 `백신 사용을 중지합니다` → `중지`는 미사용 용어, 버튼명 `사용 종료`에 맞춰 `종료합니다`로 수정
- [ ] [기존] 51~53·117~119·142~163·166~181·196~205·208~213행 불릿 기호 `*` → `-`
- [ ] [기존] 155행 `**로그**순으로` → `**로그** 순으로`(띄어쓰기)
- [ ] [기존] 156행 `**파일로 저장**순으로` → `**파일로 저장** 순으로`(띄어쓰기)
- [ ] [참고] 26행 Egress CIDR을 `114.110.144.193`으로 변경했으나 59·69·97·123행 예시 출력 URL은 `114.110.145.157` 유지 → 작성자 확인 필요(예시 갱신 여부)

## 참고
- 등급: ⓐ 변경분 = 통과 / ⓑ 문서 전체 = 수정 필요
- 6월 diff는 보안 그룹 표의 Egress CIDR 1건(IP 주소) 변경뿐이며 용어·서식 문제 없음 → ⓐ=G.
- 전체 기준으로는 `중지`(미사용 용어), `*` 점 목록 기호, `순으로` 띄어쓰기 등 기존 O급 이슈만 존재 → ⓑ=O.
- 1차 초안의 "219~223행 `*`→`-`" 항목은 해당 행이 `1\.` 번호 목록이라 오탐 → 삭제.
- `고객지원 > 문의하기`(227행) UI 경로 및 코드 블록 내 IP·경로는 정상, 수정 대상 아님.
